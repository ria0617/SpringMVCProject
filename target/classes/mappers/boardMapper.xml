<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boardMapper">
	<insert id="write">
		INSERT INTO sprmvc (BNO, TITLE, CONTENT, WRITER)
		VALUES (SPRMVC_SEQ.NEXTVAL, #{title}, #{content}, #{writer})
	</insert>
</mapper>